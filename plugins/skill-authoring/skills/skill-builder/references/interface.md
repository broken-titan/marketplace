# Interface over examples

State:

- Inputs (names, enums, optional vs required)
- Outputs (paths, status values, artifacts)
- Modes, if any (one skill, several modes — not several agents)

One short contract is enough. Add a single example only when the contract is still ambiguous.

Do not teach by gallery. Galleries become the exploration space.
