# Content craft

Purpose: get a new teammate to a running local notes app. Audience: the incoming engineer.

Clone `notes-app` and run `npm install` in the repo root.
Copy `.env.example` to `.env`. Set `PORT=3000`.
Run `npm start` and open `http://localhost:3000`.
Create one note titled `hello` and confirm it remains after refresh.
