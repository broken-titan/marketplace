---
description: Turn writing density off for this project. On by default.
---

Run `sh "${GROK_PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/scripts/writing-density-off.sh"` from the project root. Then tell the user writing density is off.
