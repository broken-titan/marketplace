---
description: Turn writing density on for this project.
---

Run `sh "${GROK_PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}/scripts/writing-density-on.sh"` from the project root. Then tell the user writing density is on. Do not start a draft unless they asked for one.
